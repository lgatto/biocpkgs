# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r_packages:
  - covr

bioc_packages:
  - BiocInstaller
  - pkgDepTools
  - graph
  - Rgraphviz
  - MSnbase

after_success:
  - Rscript -e 'covr::codecov()'
